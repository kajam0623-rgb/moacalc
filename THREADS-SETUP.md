# 스레드 자동 게시 — 마지막 한 칸

원고 생성과 게시 호출은 전부 만들어 놨다. **토큰만 채우면 완전 자동으로 돈다.**
계정 생성과 토큰 발급은 본인 확인이 필요한 일이라 직접 해야 한다.

## 1. 스레드 계정 (30초)

인스타 계정이 있으면 스레드 앱에서 바로 연결된다. 프로필은 이렇게:

```
이름   동네보살
소개   무료 사주 · 오늘의 운세. 랜덤 문구 아님, 만세력 계산.
링크   https://dongnebosal.com
```

## 2. Threads API 앱 만들기

`developers.facebook.com` → 앱 만들기 → **Threads API** 사용 사례 선택.

필요한 권한 두 개:
- `threads_basic`
- `threads_content_publish`

앱에 본인 스레드 계정을 테스터로 추가하고, 스레드 앱 설정에서 초대를 수락한다.

## 3. 토큰과 사용자 ID 받기

Graph API 탐색기나 앱의 Threads 설정에서 **장기 액세스 토큰**(60일)을 발급받는다.
사용자 ID는 아래로 확인된다.

```bash
curl "https://graph.threads.net/v1.0/me?fields=id,username&access_token=발급받은토큰"
```

## 4. 값 넣기

프로젝트 폴더에 `.env.local` 파일을 만들고 두 줄만 넣는다.

```
THREADS_USER_ID=여기에_id
THREADS_TOKEN=여기에_토큰
```

**이 파일은 `.gitignore`에 넣어 뒀다.** 커밋되지 않는다.
토큰을 채팅창이나 커밋 메시지에 붙여넣지 마라.

## 5. 확인

```bash
cd "C:/Users/닥터원츠/salary-calc" && node threads_post.js
```

미리보기만 나온다. 아무것도 올라가지 않는다. 원고가 멀쩡하면:

```bash
node threads_post.js --publish
```

## 그 다음

매일 아침 7시에 자동으로 돈다. 손댈 것 없다.

토큰은 **60일마다 갱신**해야 한다. 만료되면 게시가 실패하고 그 사실이 그대로 보고된다.

---

## 지금 상태

| | |
|---|---|
| 원고 생성 | ✅ `threads_daily.js` — 만세력으로 매일 조립 |
| 500자 가드 | ✅ 초과하면 중단 (600자로 시험함) |
| 게시 호출 | ✅ `threads_post.js` — 컨테이너 생성 → 30초 → 게시 |
| 자격증명 가드 | ✅ 토큰 없으면 중단, 원고만 출력 |
| 스케줄 | ✅ 매일 07:00 |
| **토큰** | ❌ **직접 넣어야 함** |

토큰이 없는 동안에도 스케줄은 돈다. 매일 아침 원고가 나오니 복사해서 올리면 된다.
