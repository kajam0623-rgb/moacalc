# 스레드 자동 게시 — 남은 한 칸

원고 생성·계정 확인·게시 절차는 전부 만들어 뒀다.
**계정 하나만 만들어서 로그인해두면 그때부터 손 뗄 수 있다.**

## 왜 계정 생성만 직접 해야 하나

스레드는 "누가 올리는가"를 반드시 확인한다.

- API로 가면 → 액세스 토큰이 그 증표
- 브라우저로 가면 → 로그인 세션이 그 증표

증표 없이 올리는 길은 없고, 두 증표 모두 본인 비밀번호로 로그인해야 생긴다.
그 로그인은 사용자 몫이다. 대신 **로그인만 되어 있으면 토큰은 필요 없다.**

## 할 일 (한 번만)

### 1. 계정 만들기

인스타에서 새 계정을 만들고 스레드에 연결한다. 프로필:

```
아이디  dongnebosal          ← 이미 쓰이고 있으면 아래 3번을 고친다
이름    동네보살
소개    무료 사주 · 오늘의 운세. 랜덤 문구 아님, 만세력 계산.
링크    https://dongnebosal.com
```

### 2. 그 계정으로 크롬에 로그인

자동 게시는 **이 PC의 크롬 세션**을 쓴다. 로그인 상태만 남겨두면 된다.

개인 계정과 같이 쓰고 있다면, 스레드에서 계정을 전환해 동네보살이
현재 계정이 되게 둔다. 매일 아침 게시 직전에 계정을 다시 확인하므로,
다른 계정이 켜져 있으면 게시하지 않고 멈춘다.

### 3. 핸들 등록

`.threads-account` 파일에 아이디 한 줄만 있으면 된다. `@`는 빼고.

```
dongnebosal
```

1번에서 다른 아이디를 썼다면 이 파일을 그 아이디로 고친다.
**이 값과 로그인된 계정이 다르면 게시하지 않는다.**

### 4. 한 번 수동 실행

사이드바 "Scheduled"에서 `dongnebosal-threads-daily` 를 **Run now**.
브라우저 조작 권한을 미리 승인해 두는 절차다. 이걸 해두지 않으면
매일 아침 권한 창에서 멈춘다.

## 그 다음

매일 아침 7시에 자동으로 돈다.

1. 만세력으로 그날 원고 생성
2. 로그인 계정이 `.threads-account` 와 같은지 확인
3. 같으면 게시, 다르면 중단하고 원고만 보여줌
4. 게시 후 프로필에서 실제로 올라갔는지 확인

## 지금 상태

| 항목 | 상태 |
|---|---|
| 원고 생성 | 확인함 — 실행해서 출력 봤다 |
| 500자 가드 | 확인함 — 600자로 시험, 중단 |
| 계정 불일치 가드 | 확인함 — dongnebosal vs cartoon_ani_game 로 시험, 중단 |
| 크롬 연결 | 확인함 — 로그인 세션 살아 있음 |
| 스케줄 등록 | 등록됨. 아직 실제로 돈 적 없음 |
| **동네보살 계정** | **없음 — 만들어야 함** |
| 실제 게시 | **한 번도 안 해봤다** |

## API 경로 (선택)

`threads_post.js` 는 Threads API로 올리는 경로다. 토큰을 발급받아
`.env.local` 에 넣으면 브라우저 없이도 돈다. 다만 브라우저 경로가
이미 되므로 굳이 필요하지 않다. 이 코드도 실제 게시는 아직 안 해봤다.
