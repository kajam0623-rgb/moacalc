# 스레드 자동 게시

계정: **@dongnebosal1** (숫자 없는 `dongnebosal` 은 남의 계정이다. 헷갈리지 말 것)

두 갈래가 있다. 지금 도는 건 브라우저 경로다.

| 경로 | 스크립트 | 자격증명 | 상태 |
|---|---|---|---|
| 브라우저 | `threads_browser.js` | 로그인 세션 | **작동 확인 (2026-08-13 실제 게시)** |
| API | `threads_post.js` | 액세스 토큰 | 토큰 없음. 호출된 적 없음 |

## 브라우저 경로

### 어떻게 도는가

메타 공식 **Web Intent** 를 쓴다.

```
https://www.threads.com/intent/post?text=<원고>&url=https://dongnebosal.com
```

이 주소를 열면 작성창이 원고가 채워진 채로 뜬다.
화면을 DOM으로 긁어 타이핑하지 않는다 — 스레드는 클래스명이 매번 바뀌고,
메타는 브라우저 자동 조작을 약관 위반으로 본다. Intent는 메타가 만들어 둔 문이다.

세션은 이 폴더의 전용 크롬 프로필 `.chrome-threads/` 에 남는다.
개인 크롬과 완전히 분리돼 있다. 그래서 개인 크롬에 로그인해도 여기엔 안 넘어온다.
**이 폴더는 절대 커밋하지 않는다.** 세션 쿠키가 들어 있다. (`.gitignore` 에 있음)

### 실행

```bash
node threads_browser.js --login    # 최초 1회. 창에서 직접 로그인하고 창을 닫는다
node threads_browser.js --check    # 지금 로그인된 계정만 확인
node threads_browser.js            # 원고 채운 작성창까지. 게시는 사람이 누른다  ← 기본
node threads_browser.js --publish  # 게시 버튼까지 누른다
```

### 계정 오발사 가드

자기 프로필에서만 "프로필 편집" 버튼이 뜬다는 점을 쓴다.
`.threads-account` 값과 로그인 계정이 다르면 게시하지 않고 멈춘다.
클래스명에 기대지 않으므로 화면이 바뀌어도 버틴다.

### 게시 버튼

피드 상단 작성바에도 '게시'가 있다. 그걸 누르면 빈 글 창이 열린다.
`--publish` 는 반드시 모달(`role=dialog`) 안쪽 버튼만 집는다.
누른 뒤 프로필로 가서 원고 첫 줄이 보이는지 직접 확인하고,
확인되면 `게시 확인 OK` 를 찍는다.

## 하루 두 번

| 슬롯 | 시각 | 원고 | 작업 이름 |
|---|---|---|---|
| 아침 | 07:00 | 그날 일진 (`threads_daily.js`) | `DongnebosalThreadsDaily` |
| 저녁 | 21:30 | 후킹 소재 은행 (`threads_bank.js`, 18일 주기) | `DongnebosalThreadsEvening` |

```bash
node threads_auto.js --publish --browser        # 아침
node threads_auto.js --publish --browser --pm   # 저녁
node threads_auto.js --pm                       # 게시 없이 원고만
```

`--browser` 없이 돌리면 토큰이 없어 실패한다.
중복 방지 키는 날짜+슬롯이다. 아침을 올려도 저녁은 따로 나간다.

아침 일진이 다룬 십성과 저녁 소재가 겹치면 은행이 반 바퀴 건너뛴다.
같은 날 같은 얘기를 두 번 하지 않기 위해서다.

**브라우저 경로는 화면이 필요하다.** 크롬 창이 실제로 떠야 하므로
PC가 켜져 있고 로그인된 상태여야 한다. 잠금·절전이면 그날은 걸러진다.

## 링크를 본문에 넣지 않는 이유

스레드 도달은 본문 링크에 불리하다. 2024년 페널티가 완화됐다는 발표가 있었지만
2026년에도 맥락 없는 링크는 손해라는 관찰이 우세하고, 알고리즘 1순위 신호는 체류 시간이다.

그래서 이렇게 나눈다.

- **본문**: 주소 없음. 궁금증과 "값은 안 받네"(무료)로 끝낸다
- **첫 댓글**: 주소. `reply_post_shortcode` 로 자기 글에 자동으로 단다

`--publish` 는 본문을 올린 뒤 그 글의 shortcode를 집어 댓글까지 단다.

## 약관 위험 — 읽고 결정할 것

메타는 브라우저 자동 조작을 약관 위반으로 본다.
스레드 계정은 연결된 인스타가 정지되면 같이 죽는다.

- **기본 모드**(사람이 게시 클릭)는 위험이 낮다. Intent는 공식 엔드포인트고,
  마지막 행위를 사람이 한다.
- **`--publish`**(스크립트가 클릭)는 위험이 있다. 매일 같은 시각 같은 패턴이면 더 그렇다.
- 위험이 0인 길은 API 경로뿐이다. 토큰을 발급받으면 그쪽으로 옮기는 게 맞다.

무인 스케줄러는 `--publish` 여야만 성립한다. 기본 모드는 사람 클릭이 필요해
아침 7시 자동과 맞지 않는다. 이 맞바꿈을 알고 골라야 한다.

## 한계

- **이미지 카드 첨부 안 됨.** Intent는 텍스트와 링크만 받는다.
  `card-out/*.png` 는 수동으로 끌어다 넣어야 한다.
- 500자를 넘으면 게시하지 않고 중단한다.

## 확인된 것 / 안 된 것

| 항목 | 상태 |
|---|---|
| 원고 생성 | 확인 |
| 500자 가드 | 확인 |
| 계정 불일치 가드 | 확인 — 로그인 전 상태에서 중단됨 |
| 로그인 세션 유지 | 확인 — `sessionid` 4개 디스크 기록 |
| Intent 작성창 프리필 | 확인 — 원고 전문·줄바꿈·한자·링크카드 정상 |
| **실제 게시** | **확인 — 2026-08-13, 프로필에 노출됨** |
| 저녁 은행 18편 회전 | 확인 — 7일치 돌려 같은 날·연속 중복 없음 |
| `--publish` 자동 클릭 | 코드만 있음. 이 날 게시는 별도 스크립트로 눌렀다 |
| **첫 댓글 자동 달기** | **코드만 있음. 한 번도 안 돌았다** |
| 스케줄러 정시 발화 | 미확인. 강제 실행만 해봤다 |
| API 경로 첫 댓글 | 미구현 — 공식 문서에서 파라미터를 확인하지 못했다 |
